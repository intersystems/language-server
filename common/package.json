{
  "name": "@intersystems/language-server-common",
  "version": "2.8.4-SNAPSHOT",
  "description": "Shared types and protocol definitions for InterSystems Language Server",
  "main": "out/types.js",
  "types": "out/types.d.ts",
  "private": true,
  "scripts": {
    "compile": "tsc -b",
    "watch": "tsc -b -w",
    "clean": "rimraf out"
  },
  "devDependencies": {
    "@types/node": "^18.19.50",
    "typescript": "^5.6.2"
  }
}
